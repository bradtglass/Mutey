<UserControl x:Class="Mutey.Popup.MicStatePopupControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="50">
    <Border x:Name="ContainerBorder"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Border.Background>
            <RadialGradientBrush>
                <GradientStop Offset="0" Color="#353633" />
                <GradientStop Offset="0.92" Color="#353633" />
                <GradientStop Offset="1" Color="#bfbfbf" />
            </RadialGradientBrush>
        </Border.Background>
        <Image Stretch="UniformToFill" x:Name="Image"
               Source="{Binding State, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}, Converter={StaticResource MuteStateToIconSourceConverter}}" />
    </Border>
</UserControl>